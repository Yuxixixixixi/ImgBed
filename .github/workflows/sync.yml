
name: Upstream Sync
# 该工作流已停用，原用于自动同步上游仓库变更
# 如需恢复，请取消注释 on: 并恢复 steps 内容
permissions:
  contents: write
on:
  # workflow_dispatch:  # 已禁用：关闭手动触发，彻底停用同步功能
  #   # 可选：保留但注释，便于未来启用
  #   # 可改为：push: branches: [main] 以重新启用基于推送的同步（不推荐）

jobs:
  # 已停用：原同步作业已移除
  # sync_latest_from_upstream:
  #   name: Sync latest commits from upstream repo
  #   runs-on: ubuntu-latest
  #   if: ${{ github.event.repository.fork }}
  #   steps:
  #     - name: Checkout target repo
  #       uses: actions/checkout@v3
  #     - name: Sync upstream changes
  #       id: sync
  #       uses: aormsby/Fork-Sync-With-Upstream-action@v3.4
  #       with:
  #         upstream_sync_repo: MarSeventh/CloudFlare-ImgBed
  #         upstream_sync_branch: main
  #         target_sync_branch: main
  #         target_repo_token: ${{ secrets.GITHUB_TOKEN }}
  #         test_mode: false
  #     - name: Sync check
  #       if: failure()
  #       run: |
  #         echo "[Error] 由于上游仓库的 workflow 文件变更，导致 GitHub 自动暂停了本次自动更新，你需要手动 Sync Fork 一次，详细教程请查看项目README.md "
  #         echo "[Error] Due to a change in the workflow file of the upstream repository, GitHub has automatically suspended the scheduled automatic update. You need to manually sync your fork. Please refer to the project README.md for instructions. "
  #         exit 1

  # 替代方案：添加一个仅用于提示的空作业，避免工作流文件被误删
  notify-sync-disabled:
    name: 🚫 Upstream Sync Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Show notification
        run: |
          echo "⚠️ 该自动同步功能已被永久关闭。"
          echo "如需重新启用，请联系仓库管理员并参考 README.md 中的同步指南。"
          echo "此工作流文件仅作存档用途，不再执行任何同步操作。"
